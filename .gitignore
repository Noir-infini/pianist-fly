# fetched/compiled connectome + heavy binaries — NOT in the repo
# (clone-and-go; scripts/fetch_graph.py + scripts/build_graph_npz.py reproduce data/graph.npz from the 3 pinned MaleCNS feathers)
data/*.npz
data/*.feather
data/graph-manifest.json
data/*png
data/mcns_brain_points.json
data/notes/
# runtime logs
logs/
# python
__pycache__/
*.py[cod]
.venv/
venv/
# OS
.DS_Store

