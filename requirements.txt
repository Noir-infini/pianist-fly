numpy>=2.1
pandas>=2.2
pyarrow>=17.0
mujoco>=3.1.4
dm_control>=1.0.24
# flybody (the MuJoCo fruit-fly body model) is NOT here: its upstream metadata
# pins numpy==1.26.4, which conflicts with the >=2.1 stack above (and which we
# run against fine). setup.sh installs it separately with --no-deps:
#   pip install --no-deps "flybody @ git+https://github.com/TuragaLab/flybody.git"
